// This layout is now handled by the DashboardLayout component in the page.tsx
// We're using a client-side layout for better state management

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
